<Page x:Class="hotel.view.IntroductionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="IntroductionPage"
      d:DesignHeight="680"
      d:DesignWidth="1000"
      Background="#f4f1ed"
      Loaded="Page_Loaded"
      mc:Ignorable="d">

    <Page.Resources>
        <Storyboard x:Key="SlideOutLeft">
            <DoubleAnimation Storyboard.TargetName="MainContent"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             To="-1500"
                             Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseIn" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainContent"
                             Storyboard.TargetProperty="Opacity"
                             To="0"
                             Duration="0:0:0.6" />
        </Storyboard>
    </Page.Resources>

    <Grid>
        <Button HorizontalAlignment="Left" VerticalAlignment="Top"
                Margin="30,30,0,0"
                Background="Transparent" 
                BorderBrush="Transparent"
                Cursor="Hand"
                Click="HomeButton_Click">

            <Button.Content>
                <TextBlock Text="🏠︎" FontSize="20" Foreground="#623e2a" FontWeight="Bold" FontFamily="/assets/fonts/#Coco Gothic"/>
            </Button.Content>

            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <StackPanel x:Name="MainContent"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top">
            <StackPanel.RenderTransform>
                <TranslateTransform X="0" Y="0" />
            </StackPanel.RenderTransform>

            <TextBlock x:Name="TitleText1"
                       Margin="0,50,0,0"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top"
                       FontFamily="/assets/fonts/#Catchy Mager"
                       FontSize="50"
                       Foreground="#623e2a"
                       Text="" />

            <TextBlock x:Name="TitleText2"
                       Margin="0,10,0,0"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top"
                       FontFamily="/assets/fonts/#Catchy Mager"
                       FontSize="50"
                       Foreground="#623e2a"
                       Text="" />

            <Border Width="200"
                    Height="300"
                    Margin="0,40,0,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    BorderBrush="#623e2a"
                    BorderThickness="2">
                <Grid Margin="20">
                    <Image Source="/assets/intro_image.jpg" Stretch="UniformToFill" />
                    <Rectangle Fill="#A08C6B" Opacity="0.4" />
                </Grid>
            </Border>

            <TextBlock x:Name="Subtext"
                       Width="600"
                       Margin="0,50,0,0"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top"
                       FontFamily="/assets/fonts/#Coco Gothic"
                       FontSize="14"
                       Foreground="#623e2a"
                       Text=""
                       TextAlignment="Center"
                       TextWrapping="Wrap" />

            <TextBlock x:Name="SubtextTwo"
                       Width="600"
                       Margin="0,10,0,0"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top"
                       FontFamily="/assets/fonts/#Coco Gothic"
                       FontSize="14"
                       Foreground="#623e2a"
                       Text=""
                       TextAlignment="Center"
                       TextWrapping="Wrap" />
        </StackPanel>

        <Button Width="50"
                Height="100"
                Margin="0,0,20,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Background="Transparent"
                BorderBrush="Transparent"
                Click="NextButton_Click"
                Cursor="Hand">
            <TextBlock FontFamily="/assets/fonts/#Coco Gothic"
                       FontSize="40"
                       FontWeight="Light"
                       Foreground="#623e2a"
                       Text="&gt;" />
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.6" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Page>